#!/usr/bin/node

class Rectangle {
  constructor (w, h) {
    if (!w || w <= 0 || !h || h <= 0) {
      class Rectangle {
      }
    } else {
      this.width = w;
      this.height = h;
    }
  }
}

module.exports = Rectangle;
